name: arion_client
description: Private Android and web client for the Arion music server.
publish_to: none
version: 1.4.0+5

environment:
  sdk: ^3.12.2

dependencies:
  audio_service: 0.18.19
  audio_session: 0.2.4
  crypto: ^3.0.7
  flutter:
    sdk: flutter
  http: ^1.6.0
  just_audio: ^0.10.6
  path_provider: ^2.1.5
  shared_preferences: ^2.5.5
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
